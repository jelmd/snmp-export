go:
    # Whenever the Go version is updated here,
    # .circle/config.yml should also be updated.
    version: 1.15
repository:
    path: github.com/prometheus/snmp_exporter
build:
    flags: -trimpath -a -tags netgo
    ldflags: -X 'main.Version=0.20.1'
tarball:
    files:
        - snmp.yml
        - LICENSE
        - NOTICE
